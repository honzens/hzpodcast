<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- style name="Theme.ReadTasks" parent="Theme.MaterialComponents.DayNight.DarkActionBar" -->
    <style name="Theme.HzPodcast" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 設定整體背景底色 -->
        <!-- item name="android:windowBackground">@color/app_background</item -->
        <!-- 狀態列顏色 (可選) -->
        <item name="colorPrimaryDark">@color/my_actionbar_color</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:actionMenuTextAppearance">@style/MenuTextStyle</item>
    </style>

    <style name="MenuTextStyle">
        <item name="android:textColor">@android:color/holo_red_dark</item>
        <item name="android:textSize">30sp</item>
    </style>

</resources>